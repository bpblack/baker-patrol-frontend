import { Component } from '@angular/core';

@Component({
  selector: 'baker-patrol-app',
  template: '<router-outlet></router-outlet>'
})
export class AppComponent {
  title = 'mtbakervoly-frontend';
}
